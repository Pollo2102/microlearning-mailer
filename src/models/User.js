class User {
    constructor(fullname, email, subscriptions) {
        this.fullname = fullname;
        this.email = email;
        this.subscriptions = subscriptions;
    }
}

module.exports = User;